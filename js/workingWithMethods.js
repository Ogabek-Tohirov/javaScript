'use strict'

const box = document.querySelector('#box');
const buttons = document.querySelectorAll('button');
const circle = document.querySelectorAll('.circle');
const hearts = document.querySelectorAll('.heart');
const circleWrapper = document.querySelector('.circle__wrapper');

// box.style.backgroundColor = 'green';
// box.style.height = '100px';
// box.style.width = '200px';

// box.style.cssText = 'background-color: blue; height:100px; width: 200px;';

// buttons[0].style.width = '100px';

// circle[1].style.width = '200px';

// hearts.forEach((heart) => {
//     heart.style.backgroundColor = 'blue';
// })

// for (let i of hearts) {
//     i.style.backgroundcolor = 'green';
// }

// const newButton = document.createElement('button');
// document.body.append(newButton);

// const text = document.createTextNode('Hello niggas i am new text');
// document.body.append(text);


// const newCircle = document.createElement('div');

// newCircle.classList.add('circle');
// document.body.append(newCircle);
// circleWrapper.append(newCircle)
// circleWrapper.before(newCircle);

// circle[0].before(newCircle)

// circle[1].remove();  //olib tashlaydi

// circle[0].replaceWith(newCircle); //o'rniga quyish

// newCircle.innerHTML = 'E'   //ichiga text quyish

// newCircle.textContent = 'E'  //faqat text yozish uchun

// circleWrapper.insertAdjacentHTML("beforebegin", '<pre class="circle"></pre>')


